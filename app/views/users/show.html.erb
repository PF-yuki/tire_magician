<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>

  <!-- header -->
  <%= render 'sizes/header' %>

  <main>
  <div class="container">
    <h1>マイページ</h1>
    <div class="row">
      <div class="col-xs-2"></div>
      <div class="col-xs-8">
        <h4>名前:<%= current_user.lastname %> <%= current_user.firstname %></h4>
        <h4>電話番号:<%= current_user.telephone %></h4>
        <h4>Eメールアドレス:<%= current_user.email %></h4>
        <h1><%= link_to "編集", edit_user_path(current_user.id), :style=> "text-decoration: none;" %></h1>
      </div>
    </div>
  </div>
  </main>

  <!-- footer -->
  <%= render 'sizes/footer' %>

</body>
</html>